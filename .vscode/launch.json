{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run portal server",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm start",
      "cwd": "${workspaceFolder}"
    }
  ]
}
